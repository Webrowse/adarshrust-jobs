{
	// Cloudflare Pages project that serves osspath.com. `out/` is what
	// `STATIC_EXPORT=1 npm run build` produces — plain files, no Worker.
	// Deployed by .github/workflows/deploy.yml on every push to main.
	"name": "osspath",
	"pages_build_output_dir": "out"
}
